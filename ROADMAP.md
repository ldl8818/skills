# ROADMAP

仓库导航见 `README.md`，开发规范见 `AGENTS.md`。本文只记真实进度：当前阶段、待办、阻塞、最近验证。已完成的工作以 git 历史和 PR 为档案，不在本文重复列举。

## 当前接力点

- 状态：待验证；V3 Agent 入口与文档已完成本地验证，准备提交推送；真实新 Mac 全程待验收。
- 任务：在新 Mac 用 Codex 按 AI Agent 指南完成选定环境搭建与真实验收。
- 已做到：参数化引导、系统密码／登录交还、同一状态续跑、JSON 结果和只读快照已实现；Hermes、Doraemon 必需，不迁移工作项目。私人清单附可复制接管指令。
- 验证：12项引导测试、23项 dotfiles setup 测试、87项仓库检查、元文件检查与隔离 smoke 通过；配置测试使用含空格的干净 HOME 和真实 chezmoi，系统安装及网络为模拟。
- 下一步：先推送 dotfiles 接口，再推送本仓库指南，核对远端 SHA 与 CI；在新机按 [AI Agent 新机搭建指南](self-improving/mac-environment-migration/AI-Agent新机搭建指南.md) 执行。
- 边界：未在旧机安装或部署；真实 Codex 全程驱动、系统安装、登录及新会话未验证。保留其他规则及方案改动。

## 当前阶段

四个 Skill 已纳入仓库：`lookup` 1.9.0、`self-improving` 3.1.3、`skill-manager` 2.6.5，以及尚未定版的 `download-video`。前三个已有本机运行记录；self-improving 3.1.3的 doctor 当前显示 Codex 事件覆盖5／5、Claude 0／5，Claude 新版本真实客户端验证待完成；`download-video` 已完成脚本级基础验证，实际站点下载待用户提供链接后验证。

### 并行任务合并注意

只读 Hook 修复与知识治理快照已在当前工作区集成。提交时必须保留 `self-improving/src/self_improving/hooks/readonly.py`、`hooks/common.py` 中的只读检测入口、`tests/test_readonly_guard.py` 及本次文档修正，不得用整文件复制覆盖 main。迁移前20文件隔离快照已完成完整验证；src 布局验证见下方最新记录。

## 最近完成

- 2026-09-13 12:10：补齐 V3 Codex 接管指南与无交互入口，支持参数选择、用户操作交还和同一状态续跑；已有客户端复用，Agent 不代填人工验收。准备按接口先于指南的顺序发布。

- 2026-09-13 11:45：按用户授权交付 V3 引导及冻结 Skills 配套源码，补齐 GitHub 取得说明并完成相关测试；其他审计方案、旧版迁移方案与无关规则不纳入提交。远端与 CI 结果在提交后核验。

- 2026-09-11 20:57：接力 Hook 修复已在 dotfiles 提交 e8cf77a 并推送；71项回归及真实 Codex 新会话任务名、覆盖告警接收通过。同日告警去重以真实扫描结果在隔离账本验证；本仓库仅提交本项验收记录，其他任务改动保持未提交。

- 2026-09-11 20:33：严格评审并修复新机转移方案，补齐首条命令未交付的用户入口、知识复核状态与路径绑定、准备阶段禁启动、最终一致性快照、回传业务状态后回退、下载可信性和备份边界；同步 README、迁移参考并修复新手教程审核步骤的旧源码目录。未实施恢复器、修改 dotfiles 或执行整机恢复。

- 2026-09-11 20:06：将[新机转移参考](self-improving/docs/新机转移.md) 更新为一键全量 dotfiles 恢复方案，补齐软件安装、选择性记忆迁移、两端 Token Tracker、排除 claude-mem、日志防护、配置备份、有限重试、中断续跑、业务接管及验收设计，并同步 README 入口。仅保存文档，统一恢复器尚未实现；未执行安装、配置同步或恢复，未提交、未推送。

- 2026-09-11 19:38：self-improving 3.1.3迁移至 src/self_improving，保持包名与 CLI 不变；同步打包、资源定位、源码 Hook、测试、CI 入口及6份受众文档（仓库导航核对后无需改动）。本机两端受管 Hook 已重新接线，并同步私人部署档案；保留其他任务改动，未提交、未推送。

- 2026-09-11 19:17：将 ROADMAP 开发规范入口修正为 AGENTS.md，并排查仓库同类引用；其余 CLAUDE.md 引用属于归档软链安全测试，保持不变。`git diff --check` 通过，待提交、推送。

- 2026-09-11 16:29：按用户新指令完成 Claude 真实客户端补验，当前两客户端均有3.1.2的5类事件证据。Claude 新建、恢复、清空与手动压缩的 Hook 均执行成功；新建、压缩后和清空后的模型接收得到确认。保留恢复轮模型误判重复提示来源的限制，未改模型、配置、审批账本或业务代码；未提交。

- 2026-09-11 16:21：按用户要求完成剩余验证，使用 Codex 真实客户端覆盖新建、恢复、清空和压缩后请求；Claude 按用户说明暂不验收。私人库路径告警和历史错误库已逐项核验并分类，未修改私人文档、审批账本或删除错误记录；未提交。

- 2026-09-11 16:11：完成 self-improving 定向文档同步：对齐知识目录下的索引与恢复行为、普通 Markdown 维护和审批账本边界，补齐 Skill 知识维护路由，合并重复章节及版本头。6份受众文档均已核对并同步，另更新配置、隐私参考与 CHANGELOG；知识字段参考、迁移说明及仓库导航无需改动。仅修改文档，程序版本仍为3.1.2，未提交。

- 2026-09-11 13:34：只读 Hook 误拦修复按独立补丁交付；增加前缀判断与负数下标识别，按独立命令匹配解释器引用，管道输入保持关联。保留现行普通 Markdown 维护及审批账本保护。

- 2026-09-10 19:31：按用户授权将两个 Markdown 文件恢复为普通文档维护，审批账本与变更审核命令仍受保护；同步3.1.2及6份受众文档。旧纠错原文已真实归档，哈希一致；保留此前可选档案与不重建行为，停止逐条整理历史条目。未提交。

- 2026-09-10 19:24：修复 Hook 对 Python 前缀判断、负数下标及搜索后接只读列表命令的误拦；解释器关键词按简单 Shell 命令边界匹配，复杂展开、实际写入与文件移动仍受保护。拒绝提示区分可能误拦与写入／移动／审核，不再一律指向审核命令；未提交。

- 2026-09-10 18:05：知识治理剩余验收完成；用户已替换核心短入口，Claude 使用当前 Kimi 模型真实压缩成功并恢复方法论全文与补读入口，四项判据保持。后续接力误暂停已在 dotfiles 定向修复；本任务 skills 改动尚未提交。

- 2026-09-10 17:26：部署按需知识治理3.1.0，完成版本绑定、单目录、知识预算与恢复；保留方法论原文和独立只读守卫修复。迁移处置与客户端证据记录在私人库治理实施记录，受保护核心入口及未通过的客户端事件不标为完成。未执行本任务提交。

- 2026-09-10 16:56：修复 Python 只读检查因提及受保护文件名被误拦截；使用完整命令及受限 AST 识别，未知代码、Shell 拼接、真实写入与审核继续沿用保护。同步 Hook、架构、教程和排错说明，未变更审批记录。

## 待办

### lookup · OpenCLI doctor 零窗口修复后复核版本分界

**触发条件**：上游 PR `jackwener/OpenCLI#2206` 合并、`@jackwener/opencli` 发布含该修复的版本、且本机已升级到该版本。三条同时满足才启动。

**要做的事**：

1. 改写 `lookup/references/opencli-windows.md` 的「探活别用 doctor」一节，标明版本分界——`opencli doctor` 弹前台空白窗口并永久留下的描述只适用于 1.8.6 及以前，修复版本起 `checkConnectivity()` 改用无窗口 cookies 探针，不再创建容器窗口。
2. 复核 `lookup/SKILL.md` 的「不用 `opencli doctor` 做日常探活」是否仍需保留。当前判断是保留：`scripts/selftest.sh` 另外覆盖本地依赖、OpenCLI 注册表合约和有界登录态，doctor 修好也替代不了。动手前重新确认这条理由是否还成立。

**不受影响的结论**：PR #2206 只修 doctor 一条路径，扩展的 `releaseLease()` 仍无 `chrome.windows.remove`，`automation` 容器注册表丢失后仍无法找回。因此 `opencli-windows.md` 的核心判断「容器窗口关不掉，只能靠不触发」不变，`scripts/selftest.sh` 的零窗口探针不需要改动——它与 PR 采用同一个 `cookies` 动作，且不依赖 opencli 版本。

## 阻塞

- 上游 issue `jackwener/OpenCLI#2202` 与 PR `#2206` 自 2026-07-31 起无维护者响应：零 review、零评论、CI 未运行、无 assignee。上述待办在上游合并前无法启动。

## 最近验证

- 2026-09-13 12:10：12项引导、23项 setup、87项仓库检查、元文件与隔离 smoke 均通过。干净 HOME 配置使用真实 chezmoi；系统安装和网络为模拟，真实新 Mac 全程待验收。

- 2026-09-13 11:49：源码4dec473完成远端核对，两条 CI（self-improving、skill-manager）均通过；本地78项相关测试、32项 Lookup 回归、冻结版结构检查、85文件暂存扫描及隔离安装／重复运行通过。新机实机验收仍待执行。

- 2026-09-11 21:26：全部待提交内容的隔离快照通过109项测试；3.1.3 wheel 构建成功，Python 源码逐字一致且无字节码缓存。独立虚拟环境与干净 HOME 完成版本、init、upgrade、sync --check、doctor、资源检查及保留数据卸载。新增内容公共／私人红线与敏感模式零命中，diff 检查通过。本机 sync --check 通过，doctor 显示 Codex 事件5／5、Claude 0／5；既有私人文档路径和错误库体积告警保留。本次不代表真实新 Mac 验收或统一恢复器交付。

- 2026-09-11 20:33：当前源码快照在隔离 HOME 实跑8个 CLI 步骤，先复现旧目录模块加载失败，修正文档后 init、review list、sync、doctor、upgrade 通过；重复升级 Hook 内容不变。合成数据验证复核状态缺失及路径变更均需重新复核。109项现有测试通过；4份文档公开与私人红线零命中、18个本地链接与 diff 检查通过。标点门禁仅报12处数字间距，按用户“数字与中文不加空格”规则保留。6份受众文档逐项核对：README、新手教程受影响已改，SKILL、架构、Hook、排错无程序行为变化，无需新增改动。测试证据在 `self-improving/.tmp/migration-review-20260911/`；隔离环境沿用宿主解释器，未验证发布克隆、裸机引导、真实客户端事件或业务接管，事件契约仍为0／5。

- 2026-09-11 20:06：新机转移文档16项关键内容和6个本地链接检查通过；文档与 README 公共和私人红线零命中，文档破折号检查及 `git diff --check` 通过。仅做文档静态检查；未运行安装、配置同步、恢复脚本或新机验收。

- 2026-09-11 19:38：109项测试及隔离源码109项测试、compileall、wheel 构建通过；wheel Python 文件与 src 逐字一致。移除 PYTHONPATH 后，旧 Hook 工作目录回归先失败，修正后通过；干净 HOME 源码与 wheel 均完成 init、upgrade、sync、doctor、资源检查及两端10条 Hook 命令，旧接线迁移和无关 Hook 保留通过。本机 sync／doctor 接线通过；新版本两端事件契约0／5，真实客户端未验收，既有文档路径与错误库告警保留。新增内容公共路径扫描及 diff 检查通过。

- 2026-09-11 17:38：以基线 `b2899d6` 叠加当前20文件完成隔离验证，Python 3.14.5 下109项测试及 `compileall` 通过；wheel `self_improving_memory-3.1.2-py3-none-any.whl` 构建成功并确认包含新模块、知识文档且无缓存噪声；干净 HOME 安装 smoke 的 `init`、版本输出和 `doctor` 成功；20文件公共及私人红线扫描均0命中，`git diff --check` 通过。

- 2026-09-11 16:29：Claude Code 2.1.243 沿用现役 kimi-for-coding，真实成功／失败命令分别退出0／1，doctor 当前版本事件从0／5补齐5／5。恢复轮 Hook 日志及输出均证实重发基础入口，但模型误称仅首轮收到，不能将其来源判断作为验收证据。手动 compact 完成事件记录65.525秒，SessionStart:compact 成功；压缩后模型确认基础提示，未冒充读取知识正文。clear 事件成功，清空后新请求确认基础入口；全部阶段退出0。客户端模型识别提示在成功请求中仍出现，不能据此归因失败。私人库路径与错误体积告警保留，错误库哈希未变；原始日志位于仓库外，脱敏阶段摘要在本任务临时目录。

- 2026-09-11 16:21：Codex CLI 0.154.0 与现役 self-improving 3.1.2 实测：新会话收到基础提示；app-server 恢复时5类受管事件均完成，成功／失败命令分别退出0／1；真实压缩完成后下一请求重新收到预算不足的知识补读提示，模型未冒充读过原文；TUI 清空后新请求收到基础提示。4项错误捕获／持久化回归通过。13项路径告警分为4项模板或通配符、8项引用基准目录不一致（按语义定位的实际文件存在）、1项历史示例已无现役文件。错误库3486条、最新日期2026-08-31，当前捕获关闭，核验前后哈希一致；上限仅在追加新错误时执行，doctor 不裁剪历史数据。告警保留，Claude 不以 Codex 结果替代验收；脱敏结果在本任务临时目录，原始客户端记录留在仓库外。

- 2026-09-11 16:11：定向文档同步通过109项测试、Skill 校验、11份文档公开红线扫描、27个本地文件链接检查及 diff 检查。干净 HOME 按教程完成 init、双客户端5类 Hook 接线、sync 和 doctor；公开 Skill 内的记忆目录按设计被拒绝。现役 sync／doctor 的索引与知识版本通过，Claude 事件0／5、Codex 5／5；私人库路径与错误库3486／200条告警保留，未进行真实客户端完整验收。隔离安装日志与环境定位保留在本任务临时目录，未清场。

- 2026-09-11 13:34：仅含本次补丁的隔离快照87项测试通过；原误拦4处红灯、管道回归1处红灯均先复现再修复。未纳入知识治理改动，安装包只读识别 smoke 通过；远端 CI 以对应提交运行结果为准。

- 2026-09-10 19:31：完整108项测试通过，覆盖普通文档维护放行与审批保护；Codex 工具通道真实归档成功，原文哈希不变。sync及doctor来源版本与索引检查通过；新版本事件Codex 2／5、Claude 0／5，新会话及恢复验收待验证；原有错误库及项目路径告警保留。

- 2026-09-10 19:24：两类误拦新增回归先出现4处失败，修复后完整107项测试通过；当前工具通道实跑纠错表只读检查和搜索＋生命周期列表均成功。sync 与 diff 检查通过；doctor 保留客户端事件覆盖、已有路径占位、错误库体积及4项知识版本待核对告警，未更改审批记录或执行历史归档移动。

- 2026-09-10 18:59：完成私人旧纠错逐项归位核对与删除影响判断，区分已覆盖、现行替代和未归位专项；确认 Git 可恢复原文及体检、初始化、历史导入依赖。仅记录判断，未删除旧档案或修改现役规则；此前入口优化稿不再作为推荐执行路径。

- 2026-09-10 18:45：三个独立角色复核 self-improving 索引、历史纠错与维护方案；索引一致性检查通过，区分生成导航与运行时知识路由。发现候选误收、生命周期列表不等于当前注入、一次性应用脚本失败重试不足；仅记录评审建议，未改机制、审批或应用私人文档。

- 2026-09-10 18:36：针对 self-improving 3.1.0 的纠错档案与机制核验，现役104项隔离测试通过，知识目录与生成索引复核通过；直接核对两客户端当天压缩后的知识接收记录。Codex 事件契约5／5，Claude 3／5，后者缺当前版本前后工具事件；未模拟真实用户批准。私人纠错档案入口优化被写入守卫拒绝，优化稿及普通终端应用脚本已准备，尚未应用；未更改审批账本或客户端配置。

- 2026-09-10 18:05：Claude 真实压缩边界及 SessionStart:compact 已记录；第二次压缩明确输出 agent-method 全文补读入口，客户端恢复的原文与磁盘逐字一致，后续只读回答四项判据准确。首个非交互调用120秒超时不再作为当前阻塞；模型提示在成功普通请求也出现，不能单凭提示归因。

- 2026-09-10 17:56：用户更新后的核心入口与获批替换稿逐字一致，归档及方法论哈希不变；sync／doctor 对应核心检查通过，架构维护来源已复核。Kimi 普通请求约7秒成功，旧会话非交互压缩120秒未完成；真实压缩验收继续以客户端完成记录为准。

- 2026-09-10 17:26：现役104项测试、最新 wheel 隔离安装22项知识回归、公开红线扫描及 diff 检查通过；知识目录20条来源均 ready。Codex 真实压缩后收到全文补读入口并实际读取；Claude 压缩55秒无结果，未改其模型配置。doctor 的旧核心、历史错误库和项目路径告警保留。

- 2026-09-10 17:05：提交前隔离验证只读守卫；补充排序与 JSON 回调的动态执行边界，回归先复现后修复。另一知识治理任务的代码、版本和文档变更不纳入本次提交。

- 2026-09-10 16:56：新增只读保护回归测试先复现4处失败，修复后本版本81项通过；与 knowledge-governance 快照组合102项通过。实际 Codex Hook 放行 Python 只读检查，写入／删除／改名／动态执行／审批保护由测试验证。`git diff --check` 通过。`sync --check` 发现现有知识索引差异，doctor 另报旧引用、核心记忆时效和错误库体积告警；留给并行知识治理任务，本次未重写私人索引。

- 2026-09-10 00:37：新增 `download-video` Skill，已通过 Python 编译检查和合成参数／核心函数 smoke；未进行实际站点下载验证。
